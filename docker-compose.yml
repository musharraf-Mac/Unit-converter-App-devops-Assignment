version: '3.8'

services:
  web:
    build: .
    container_name: unit-converter
    ports:
      - "8080:80"
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge